import React from 'react'
import ReservationsList from './ReservationsList'

const Reservations = () => {

  return (
    <section>
      <ReservationsList />
    </section>
  )
}

export default Reservations